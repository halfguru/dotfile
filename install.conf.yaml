- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/bottom/bottom.toml: bottom.toml
    ~/.config/nvim: neovim
    ~/.gitconfig: gitconfig
    ~/.zshenv: zshenv
    ~/.zshrc: zshrc
